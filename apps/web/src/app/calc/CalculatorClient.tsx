"use client";

export default function CalculatorClient() {
  // Minimal client wrapper to satisfy the import from page.tsx.
  // You can replace this with the richer UI once we wire sections.
  return (
    <main className="p-6 space-y-4">
      <h1 className="text-2xl font-semibold">__REPLACE_ME::HEADING__</h1>
      <p className="text-sm opacity-80">
        Calculator Playground is ready. Add sections and math panels here.
      </p>
    </main>
  );
}
